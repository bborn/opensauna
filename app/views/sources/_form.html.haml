= form_for [@dashboard, @source] do |f|
  -if @source.errors.any?
    #error_explanation
      %h2= "#{pluralize(@source.errors.count, "error")} prohibited this source from being saved:"
      %ul
        - @source.errors.full_messages.each do |msg|
          %li= msg
  .field
    =f.label :name
    %br
    =f.text_field :name
  .actions
    = f.submit 'Save'
